#include<stdio.h>
int  main(){ 
    int n,pos;
    printf("Enter the number ");
    scanf("%d",&n);
    printf("Enter the position");
    scanf("%d",&pos);
    int r=n>>pos&1;
    if(r){
    printf("the number is on");}
    else{
       printf(" the number is off");
    }
    return 0;
    
}
