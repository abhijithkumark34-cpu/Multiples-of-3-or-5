// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int n,i;
    printf("Enter the number of elemants");
    scanf("%d",&n);
    
    int s[n];
    printf("Enter all the elements");
    for(i=0;i<n;i++){
        scanf("%d",&s[i]);
    }
    for(i=0;i<n;i++){
                    int flag=0;
        for(int j=0;j<n;j++){

        if(s[i]==s[j]){
            flag++;
        }
       
            
        }
        
        if(flag==1){
            printf("The number is %d",s[i]
            );
            
        }
    }
    
    return 0;

}
