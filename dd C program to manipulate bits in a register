// Online C compiler to run C program online
#include <stdio.h>
int main(){
    int reg=0;
    reg|=(1<<2);
    reg|=(1<<4);
    printf("the reg is %d\n",reg);
    //clear
    reg&=~(1<<4);
    printf("reg=%d",reg);
}
